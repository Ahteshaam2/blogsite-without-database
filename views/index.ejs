<%-include('header.ejs') %>
<div class="container">
<h1>home</h1>
<p><%=content %></p>
</div>
<% posts.forEach(function(post){ %>
    <div class="container">
    <h1><%=post.title %></h1>
    <p><%=post.postcontent.substring(0,10) + "...." %></p>
<a href="/post/<%=post.title  %>"> Read More</a></div>
 <% }) %>


<%-include('footer.ejs')%>
